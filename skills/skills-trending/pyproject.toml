[project]
name = "skills-trending-scraper"
version = "0.1.0"
description = "Headless browser scraper for skills.sh trending data"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
]

[dependency-groups]
dev = []
